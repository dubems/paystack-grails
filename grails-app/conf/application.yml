---
grails:
    profile: web-plugin
    codegen:
        defaultPackage: paystack.grails
    spring:
        transactionManagement:
            proxies: false
info:
    app:
        name: '@info.app.name@'
        version: '@info.app.version@'
        grailsVersion: '@info.app.grailsVersion@'
spring:
    main:
        banner-mode: "off"
    groovy:
        template:
            check-template-location: false

---
grails:
    mime:
        disable:
            accept:
                header:
                    userAgents:
                        - Gecko
                        - WebKit
                        - Presto
                        - Trident
        types:
            all: '*/*'
            atom: application/atom+xml
            css: text/css
            csv: text/csv
            form: application/x-www-form-urlencoded
            html:
              - text/html
              - application/xhtml+xml
            js: text/javascript
            json:
              - application/json
              - text/json
            multipartForm: multipart/form-data
            pdf: application/pdf
            rss: application/rss+xml
            text: text/plain
            hal:
              - application/hal+json
              - application/hal+xml
            xml:
              - text/xml
              - application/xml
    urlmapping:
        cache:
            maxsize: 1000
    controllers:
        defaultScope: singleton
    converters:
        encoding: UTF-8
    views:
        default:
            codec: html
        gsp:
            encoding: UTF-8
            htmlcodec: xml

            codecs:
                expression: html
                scriptlets: html
                taglib: none
                staticparts: none
#paystack:
##    liveSecretKey: '${PAYSTACK_LIVE_SECRET_KEY}'
#    liveSecretKey: '${PAYSTACK_LIVE_SECRET_KEY}'
##    testSecretKey: '${PAYSTACK_TEST_SECRET_KEY}'
#    testSecretKey: 'sk_test_f9b3d4c83b660c8f1bc8e25e4d0e55c8d3694923'
##    livePublicKey: '${PAYSTACK_LIVE_PUBLIC_KEY}'
#    livePublicKey: '${PAYSTACK_LIVE_PUBLIC_KEY}'
##    testPublicKey: '${PAYSTACK_TEST_PUBLIC_KEY}'
#    testPublicKey: 'pk_test_582ad59d5d94ebe5f6fb47ea05727a4a63f9ae7d'
##    endpoint:      '{PAYSTACK_ENDPOINT}'
#    endpoint:      'https://api.paystack.co'
